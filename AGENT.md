# {{PROJECT_NAME}} 작업 컨텍스트

> 새 프로젝트에 배치할 규칙 파일 템플릿.
> `{{...}}` 플레이스홀더를 채우고, 프로젝트에 맞지 않는 절은 지운다.
> 도구에 따라 파일명을 바꿔 쓴다 (예: `CLAUDE.md`, `AGENTS.md`).

## 프로젝트 구성

다음 디렉토리는 **하나의 논리적 프로젝트**다.

- `{{REPO_1}}` — {{설명}} — **수정 가능**
- `{{REPO_2}}` — {{설명}} — **수정 가능**
- `{{REPO_3}}` — {{설명}} — **🔒 읽기 전용** (담당자가 따로 있음. 참조용 조회만, 수정 금지. 변경이 필요하면 코드를 고치지 말고 무엇을 어떻게 바꿔야 하는지 정리해 전달)

이슈 분석 시 **모든 레포를 후보 컨텍스트로 고려**한다. 한두 개만 보고 결론 내리지 않는다.

---

## 사전 분석 컨텍스트 — `.agent-context/`

사전 분석 결과가 이 디렉토리에 누적된다. **새 문의가 들어오면 코드를 직접 훑기 전에 반드시 여기부터 본다.**

### Layer 1 — 광범위·얕음 (초기 1회 생성)
- `architecture.md` — 레포별 책임, 기술 스택, 호출 흐름
- `api-catalog.md` — 전체 API 한 줄 카탈로그
- `domain-map.md` — 핵심 엔티티와 관계
- `glossary.md` — 도메인 용어

### Layer 2 — 영역별 깊은 분석 (처리 중 자동 누적)
- `deep/{영역}-{흐름}.md` — 한 번 깊이 판 영역은 버리지 않는다. 재문의 시 즉답

### 기록
- `qa-log/YYYY-MM-DD-{slug}.md` — 처리한 문의와 답변
- `tickets/{KEY}.md` — 추적 건 (원문 + 진행 상태 + 체크리스트)
- `_INFLIGHT.md` — 병렬 세션 관제 보드 (append 위주)

---

## 문의 처리 순서 (필수)

0. **분류 먼저** — 즉답형(문의) / 추적형(개선) / 분석 중 하나로 가른다
1. **`qa-log/` 검색** — 유사 과거 건이 있으면 그 답을 베이스로
2. **`deep/`·진단 레시피 검색** — 관련 영역 분석이 있으면 재활용
3. **Layer 1 조회** — 인덱스에서 시작점을 찾는다
4. **타겟 코드만 확인** — 인덱스가 가리키는 파일만. **전체 레포 콜드 스캔 금지**
5. **저장** — 새 분석이 생겼으면 `qa-log/`에, 영역 흐름 분석이면 `deep/`에도

### 지식 미스 처리 (miss loop)
지식에서 못 찾았을 때:
1. 인덱스만 없는 건지 실제로 없는 건지 타겟 확인
2. 있으면 → `deep/`에 쌓는다. 다음엔 즉답
3. **없으면 → "없음"도 기록한다.** 같은 막다른 길 재탐색 방지

### 인덱스가 코드와 어긋날 때
코드를 신뢰하고 인덱스를 고친다.

---

## 두 개의 레인

**즉답형(문의)** — 재발성·패턴형이다. 속도가 전부.
- 처리: `qa-log/` + 진단 레시피 매칭 → 즉답
- 새로 풀었고 재발 가능성이 있으면 → **진단 레시피**로 저장
- 레시피 형식: `deep/voc-{증상}.md`
  ```
  증상 → 확인할 데이터·로그 순서 → 자사 vs 외부 책임 분기 → 검증 절차
  ```

**추적형(개선)** — 전체 로직 파악이 필요해 당장 못 할 수 있다. 그래도 응답성은 유지한다.
- **들어온 즉시 `tickets/{KEY}.md` 등록.** 로직 파악 전이라도 기록부터 (블로킹 금지)
- 파악은 점진적으로: `가설 → 부분확인 → 확정`
- **수확** — 운영 반영되면 파악한 로직을 `deep/`로 졸업시킨다.
  "내가 뭘 고쳤나"(커밋 레벨)가 아니라 **"이 영역이 어떻게 동작하나"(동작 레벨)** 로 쓴다.
  - ❌ "XxxService:1215 에 파라미터 추가" — 코드 바뀌면 썩고 재사용 안 됨
  - ✅ "저장 시 암호화, 발송 시 복호화, 탈퇴자는 수신 목록에서 제외" — 다음 문의에 그대로 쓰임

---

## 신뢰도 표시 (필수)

**모든 지식 항목 첫 줄에 상태를 박는다.**

- `상태: 확정` — 끝까지 추적해 검증 완료
- `상태: 부분확인` — 코드는 봤으나 실제 검증 전, 또는 일부 경로만 확인
- `상태: 가설` — 추론 단계

**기본값**: 상태 표시가 없는 항목은 **미검증으로 취급**한다.
**파일 단위 상태**는 그 안에서 **가장 약한 주장** 기준. 불확실한 개별 문장은 인라인으로 `(추정)` 표시.
**상태 없는 단정 금지.**

### 확정에는 재실행 가능한 검증 줄을 의무화
- `확정` 항목에는 `검증: <재실행 가능한 절차> / 기대: <값>` 한 줄을 반드시 붙인다
- **못 만들면 확정 금지** → `부분확인`에 머문다
- **verify-on-use** — 확정 사실이 답변의 근거일 때만 검증 줄을 재실행한다.
  스쳐가는 참조는 넘어간다. **주기적 전수 감사는 하지 않는다**

### 신선도
- `file:line`이나 커밋 해시를 인용하는 확정은 근거로 쓰기 전 현재 코드와 1회 대조
- **데이터 기반 확정**(한도값, 설정값 등 코드가 안 바뀌어도 틀려지는 것)은 검증 절차를 재실행
- 오래된 확정도 무조건 신뢰하지 않는다

### 격하 (승격의 역방향)
반증을 발견하면 **지우지 말고 격하한다.**
1. `상태: 반증(YYYY-MM-DD)` 으로 바꾸고 취소선 + **왜 틀렸나** + **올바른 값**을 남긴다
2. 그 항목을 **직접 인용**한 문서를 찾아 `부분확인(상류 반증으로 재검토 필요)`으로 강등.
   **한 홉만. 재귀 전파 안 함**
3. 인덱스에 반증 표기

---

## 자율 수준 (이층 게이트)

**낮은 위험 → 묻지 말고 자동**
- 읽기와 검색
- 새로 안 것을 `부분확인`/`가설`로 저장 (잠정 표시가 박히므로 오염 위험 없음)
- 추적 건 즉시 등록, 보드 갱신
- **틀린 확정의 격하** (신뢰를 낮추는 방향이라 안전)

**높은 위험 → 실행 전 반드시 확인**
- **`확정` 승격** 및 `deep/` 졸업
- 확정 항목의 **완전 삭제**
- **커밋과 배포**, 운영에 영향 있는 코드 변경
- 인덱스 대규모 재편

요컨대 **잠정 지식은 알아서 쌓고, "확정·운영·커밋"만 사람이 누른다.**

---

## 메모리 라이프사이클

**진행 메모리**(`tickets/`) = "내가 지금 뭘 하는 중". 닫히면 줄어든다.
**동작 레퍼런스**(`deep/`) = "시스템이 이렇게 동작한다". 휘발성 디테일은 최소화한다.

### 졸업 (운영 반영 + 확정 검증 완료 시)
1. `deep/`에 확정된 동작 지식으로 통합. **재실행 가능한 검증 줄 필수 — 없으면 졸업 금지**
2. 진행 메모리를 `상태: 졸업(→ deep/xxx.md)` 으로 바꾸고 짧은 후기 + 포인터로 트림
3. 인덱스 갱신

---

## 병렬 세션 운영

문의마다 세션을 열어 병렬 처리하면 세션끼리 서로를 모른다. 공용 보드로 간접 연결한다.

- **작업을 받으면 즉시** 보드에 자기 줄 1줄 append. **남의 줄 수정 금지**
- **세션 시작 시** 보드와 `qa-log/`를 먼저 확인 → 중복 문의와 연속 맥락을 그때 잡는다
- **끝나면** 자기 줄을 완료로 바꾸고 기록으로 졸업

```bash
scripts/board.sh add "레인" "요약"    # 작업 받으면
scripts/board.sh done "키워드"        # 끝나면
scripts/board.sh show                 # 최신 상태 확인
```

세션 시작 훅이 보드를 자동 주입한다. 단, 훅은 **시작 시점 스냅샷**이므로 새 작업 등록 전 `show`로 한 번 확인한다.

### 충돌 방지
- **같은 워킹트리를 두 세션이 동시에 수정·체크아웃·커밋 금지.** 레포 단위로 나누거나 worktree로 분리
- 단일 파일(보드, 인덱스)은 **append 위주**로. 주기적으로 중복 정리

---

## 데이터 확인 정책

- 운영 데이터 직접 접근 **금지**
- 확인이 필요하면 **조회 쿼리만 생성**해서 전달 → 사용자가 실행하고 결과를 붙여넣으면 해석
- 모든 생성 쿼리에 건수 제한 포함, 읽기 전용 가정

## 개인정보 처리

- 개인정보 컬럼과 필드를 식별해 다룬다
- 평문 저장·로깅·노출 의심 지점은 즉시 경고
- 변경 영향도는 **모든 레포**에서 호출 체인 끝까지 추적

## 금지·주의

- 전체 레포 콜드 스캔 금지 (인덱스 우선)
- 외부 도구 자발적 사용 금지 (읽기 전용 조회는 예외로 허용 가능)
- 변경 영향이 불확실하면 **실행 전 반드시 확인**
